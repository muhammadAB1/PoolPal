# Pool Test Strip Chart — Fixed Squares / Labels Above

Use `pool_chart_fixed_squares_labels_above_editable.svg` as the master editable file.

This version preserves the exact swatch colors and range indications from the first source image, while adapting the layout to more closely match the second reference image: square swatches, labels above each row, and more vertical spacing for clearer range indications.

Every swatch is a separate SVG rectangle, and every bracket is a separate vector path. Adobe Express may recolor imported SVG graphics, but Illustrator, Figma, or Inkscape give more reliable one-square-at-a-time editing.
